hk_room_scene:
  default:
  card:
    type: button
    name: "[[name]]"
    icon: "[[icon]]"
    tap_action:
      action: call-service
      service: scene.turn_on
      service_data:
        entity_id: "[[entity_id]]"
    hold_action:
      action: more-info
    show_icon: true
    show_name: true
    style: |
      ha-card {
        margin: -5px 3px 10px 14px;
      }